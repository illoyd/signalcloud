-# Page header
- headings 'Phone Numbers', 'all phone numbers', :phone_numbers
- breadcrumb :organization_phone_numbers, @organization

%table.list
  %thead
    %tr
      %th
      %th Phone Number
      %th
        = icon(:phone_books)
        Phone Books
      %th.text-center Status
      %th.text-right Monthly fee
  %tbody
    = render(partial: 'phone_number', collection: @phone_numbers) || render(partial: 'no_phone_numbers')
