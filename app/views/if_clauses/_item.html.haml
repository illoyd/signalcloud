- cache item do
  %li
    = render(item)
    = item.edit_with_modal
    = item.edit_modal
    = item.destroy_with_modal
    = item.destroy_modal

    - %w( ThenSendMessageClause ).each do |klass|
      - klass = klass.constantize
      = ThenClauseDecorator.decorate(klass.new(if_clause: item)).new_with_modal(iconify('Message', :new))
      = ThenClauseDecorator.decorate(klass.new(if_clause: item)).edit_modal
    = render(partial: 'then_clauses/then_clauses', object: item.then_clauses)    
