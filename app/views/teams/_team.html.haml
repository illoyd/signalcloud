- cache [team, policy(team).show?, policy(team).edit?] do
  %tr
    %td= team.link_to
    %td= team.active_checkmark
    %td
      = team.owner.gravatar_image_tag(20, class: 'img-circle')
      = team.owner.link_to
    %td= team.description
    %td.tools
      = team.show_button
      = team.edit_button
