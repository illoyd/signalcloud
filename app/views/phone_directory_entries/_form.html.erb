<%= form_for(@phone_directory_entry) do |f| %>
  <% if @phone_directory_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone_directory_entry.errors.count, "error") %> prohibited this phone_directory_entry from being saved:</h2>

      <ul>
      <% @phone_directory_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :directory %><br />
    <%= f.text_field :directory %>
  </div>
  <div class="field">
    <%= f.label :phone_number %><br />
    <%= f.text_field :phone_number %>
  </div>
  <div class="field">
    <%= f.label :country %><br />
    <%= f.text_field :country %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
