= form_for( @account, :html => { :class => 'form-horizontal' } ) do |f|
  - if @account.errors.any?
    #error_explanation
      %h2
        = pluralize(@account.errors.count, "error")
        prohibited this account from being saved:
      %ul
        - @account.errors.full_messages.each do |msg|
          %li= msg

  .control-group
    = f.label :label, :class => 'control-label'
    .controls= f.text_field :label

  .control-group
    = f.label :account_plan, :class => 'control-label'
    .controls= f.collection_select :account_plan_id, AccountPlan.all, :id, :label

  .control-group
    .controls
      = f.submit :class => 'btn btn-primary'
      = link_to 'Discard changes', account_path, :class => 'btn'