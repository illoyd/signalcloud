- cache if_response_clause.model do
  .modal.fade[if_response_clause.model, :destroy]
    .modal-dialog
      .modal-content
        .modal-header
          %h4.modal-title= iconify('Remove', :delete)
        .modal-body
          %p Are you sure you want to remove <strong>#{ if_response_clause.name }</strong> from <strong>#{ if_response_clause.parent.name }</strong>?
          %p No worries - you can undo this by re-adding the action.
        .modal-footer
          = link_to iconify('Remove', :delete), if_response_clause, method: :delete, class: 'btn btn-danger'
          %a.btn.btn-default{"data-dismiss" => "modal", :type => "button"}= iconify('Keep', :cancel)
