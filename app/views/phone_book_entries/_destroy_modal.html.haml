- cache phone_book_entry.model do
  .modal.fade[phone_book_entry.model, :destroy]
    .modal-dialog
      .modal-content
        .modal-header
          %h4.modal-title= iconify('Remove', :delete)
        .modal-body
          %p Are you sure you want to remove <strong>#{ phone_book_entry.phone_number.link_to }</strong> from <strong>#{ phone_book_entry.phone_book.link_to }</strong> for <strong>#{ phone_book_entry.country || 'Global' }</strong>?
          %p No worries, this only removes the phone number from this phone book. You can undo this by re-adding the phone number to the phone book again.
        .modal-footer
          = link_to iconify('Remove', :delete), phone_book_entry, method: :delete, class: 'btn btn-danger'
          %a.btn.btn-default{"data-dismiss" => "modal", :type => "button"}= iconify('Keep', :cancel)
