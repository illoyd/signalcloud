# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/Fixtures.html
<%
test_conversation_challenge_payload = { body: 'Hi!', to: 'us', from: 'them', price: 0.01 }
test_conversation_response_payload = { body: 'Hi!', to: 'us', from: 'them', price: 0.01 }
test_conversation_reply_payload = { body: 'Hi!', to: 'us', from: 'them', price: 0.01 }
%>

test_conversation_challenge:
  conversation: test_conversation
  provider_cost: 0.01
  our_cost: 0.02
  twilio_sid: "<%= 'A1' + SecureRandom.hex(16) %>"
  encrypted_payload: "<%= Message.encrypt(:payload, test_conversation_challenge_payload) %>"
  message_kind: "<%= Message::CHALLENGE %>"

test_conversation_response:
  conversation: test_conversation
  provider_cost: 0.01
  our_cost: 0.02
  twilio_sid: "<%= 'A2' + SecureRandom.hex(16) %>"
  encrypted_payload: "<%= Message.encrypt(:payload, test_conversation_response_payload) %>"

test_conversation_reply:
  conversation: test_conversation
  provider_cost: 0.01
  our_cost: 0.02
  twilio_sid: "<%= 'A3' + SecureRandom.hex(16) %>"
  encrypted_payload: "<%= Message.encrypt(:payload, test_conversation_reply_payload) %>"
  message_kind: "<%= Message::REPLY %>"

dedicated_conversation_challenge:
  conversation: dedicated_conversation
  provider_cost: 0.01
  our_cost: 0.02
  twilio_sid: "<%= 'A1' + SecureRandom.hex(16) %>"
  encrypted_payload: "<%= Message.encrypt(:payload, test_conversation_challenge_payload) %>"
  message_kind: "<%= Message::CHALLENGE %>"

