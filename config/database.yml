# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  # adapter: sqlite3
  adapter: postgresql
  encoding: utf8
  # database: db/development.sqlite3
  database: ticketplease_dev
  pool: 5
  timeout: 5000
  host: localhost
  user: ian

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  # adapter: sqlite3
  adapter: postgresql
  encoding: utf8
  # database: db/test.sqlite3
  database: ticketplease_test
  pool: 5
  timeout: 5000
  host: localhost
  user: ian

production:
  # adapter: sqlite3
  adapter: postgresql
  encoding: utf8
  # database: db/production.sqlite3
  database: ticketplease
  pool: 5
  timeout: 5000
